//Require express router.
const router = require('express').Router();

//Set up requirements from the User controller.

//Set up GET route to get all users and POST route to create a user.

//Set up a GET route to get a user by id, PUT route to update a user and DELETE route to delete a user.

//Set up a POST route to add a friend and a DELETE route to delete a friend.

//Export the router.
module.exports = router;